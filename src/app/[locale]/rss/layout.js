export const metadata = {
  title: 'RSS Feeds',
  description: 'Manage your RSS feeds and automate TorBox downloads from feed items.',
};

export default function RssLayout({ children }) {
  return children;
}
