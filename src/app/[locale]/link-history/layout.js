export const metadata = {
  title: 'Link History',
  description: 'Browse your TorBox link history and past downloads.',
};

export default function LinkHistoryLayout({ children }) {
  return children;
}
