# TorBox Backend Configuration

# TorBox API Key (optional - can be set via frontend instead)
# TORBOX_API_KEY=your_torbox_api_key_here

# Frontend URL (for CORS - only needed if not using localhost:3000)
# FRONTEND_URL=http://localhost:3000

