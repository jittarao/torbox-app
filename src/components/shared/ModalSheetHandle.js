/** Drag handle shown at the top of bottom-sheet modals on mobile only. */
export default function ModalSheetHandle() {
  return (
    <div className="ui-modal-sheet-handle" aria-hidden>
      <div className="ui-modal-sheet-handle-bar" />
    </div>
  );
}
